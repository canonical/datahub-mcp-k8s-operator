# Copyright 2026 Canonical Ltd.
# See LICENSE file for licensing details.

[tox]
no_package = True
skip_missing_interpreters = True
env_list = fmt, lint, static, unit, serve, coverage-report
min_version = 4.0.0
max-line-length=120

[vars]
src_path = {tox_root}/src
tests_path = {tox_root}/tests
all_path = {[vars]src_path} {[vars]tests_path}

[testenv]
runner = uv-venv-runner
set_env =
    PYTHONPATH={tox_root}/lib:{[vars]src_path}
    PYTHONBREAKPOINT=pdb.set_trace
    PY_COLORS=1
pass_env =
    PYTHONPATH
    CHARM_BUILD_DIR
    MODEL_SETTINGS

[testenv:fmt]
description = Format the code
dependency_groups = lint
commands =
    isort {[vars]src_path} {[vars]tests_path}
    black {[vars]src_path} {[vars]tests_path}

[testenv:lint]
description = Lint the code
dependency_groups = lint
commands =
    pydocstyle {[vars]src_path}
    codespell {toxinidir} --skip {toxinidir}/.git --skip {toxinidir}/.tox \
      --skip {toxinidir}/build --skip {toxinidir}/lib --skip {toxinidir}/venv \
      --skip {toxinidir}/.mypy_cache --skip {toxinidir}/icon.svg \
      --skip {toxinidir}/uv.lock \
      -L unsecure
    pflake8 {[vars]src_path} {[vars]tests_path}
    isort --check-only --diff {[vars]src_path} {[vars]tests_path}
    black --check --diff {[vars]src_path} {[vars]tests_path}
    mypy {[vars]all_path} --ignore-missing-imports --follow-imports=skip --install-types --non-interactive
    pylint {[vars]src_path} {[vars]tests_path} --disable=E0401,W1203,W0613,W0718,R0903,W1514,C0103,R0913,C0301,W0212,R0902,C0104,W0640,R0801,W0511,R0914,R0912,E1120,W0621,C1803,R0917


[testenv:unit]
description = Run unit tests
deps =
    -r pyproject.toml
dependency_groups = unit
commands =
    coverage run --source={[vars]src_path} \
                 -m pytest \
                 --tb native \
                 -v \
                 -s \
                 {posargs} \
                 {[vars]tests_path}/unit
    coverage report

[testenv:serve]
description = Run unit tests for the rock entrypoint
dependency_groups = serve
commands =
    pytest --tb native -v -s {posargs} {[vars]tests_path}/serve

[testenv:coverage-report]
description = Create test coverage report
depends = unit
deps =
    -r pyproject.toml
dependency_groups = unit
commands =
    coverage report

[testenv:static]
description = Run static analysis tests
deps =
    -r pyproject.toml
dependency_groups = static
commands =
    bandit -c {toxinidir}/pyproject.toml -r {[vars]src_path} {[vars]tests_path}

[testenv:integration]
description = Run integration tests
runner = uv-venv-lock-runner
pass_env =
    CHARM_PATH
dependency_groups = integration
commands =
    pytest -v \
           -s \
           --tb native \
           --log-cli-level=INFO \
           {posargs} \
           {[vars]tests_path}/integration
